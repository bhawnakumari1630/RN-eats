export interface CoordinatesType {
  lat: string | number;
  lng: string | number;
}

export interface AddressType {
  address: any;
  addresstype: string;
  class: string;
  display_name: string;
  lat: string | number;
  lon: string | number;
}
